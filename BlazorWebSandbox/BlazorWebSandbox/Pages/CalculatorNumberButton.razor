<button class="col-4" @onclick="@(()=>SelectNumber())">@Value</button>

@code {
    [Parameter]
    public int Value { get; set; }

    [Parameter]
    public EventCallback<int> OnButtonClick { get; set; }

    private async Task SelectNumber()
    {
        await OnButtonClick.InvokeAsync(Value);
    }
}
